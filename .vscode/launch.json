{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "Launch Jekyll with Browser",
          "type": "msedge",
          "request": "launch",
          "url": "http://localhost:4000",
          "webRoot": "${workspaceFolder}",
          "preLaunchTask": "Start Jekyll"
      }
  ]
}
